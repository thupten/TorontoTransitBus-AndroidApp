<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thuptencho.torontotransitbus"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.thuptencho.torontotransitbus.provider.TheContentProvider"
            android:authorities="@string/authority"
            android:exported="false" />
        
        <service
            android:name="com.thuptencho.torontotransitbus.backgroundservice.DatabaseUpdatingService"
            android:label="@string/service_label" >
        </service>

        <activity
            android:name="com.thuptencho.torontotransitbus.routes.RouteDetailFragmentActivity"
            android:label="@string/title_activity_route_detail" >
        </activity>
    </application>

</manifest>